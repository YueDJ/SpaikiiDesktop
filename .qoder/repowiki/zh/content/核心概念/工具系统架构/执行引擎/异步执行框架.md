# 异步执行框架

<cite>
**本文引用的文件**
- [tools/registry.py](file://tools/registry.py)
- [agent/conversation_loop.py](file://agent/conversation_loop.py)
- [agent/async_utils.py](file://agent/async_utils.py)
</cite>

## 目录
1. [简介](#简介)
2. [is_async 标志设计](#is_async-标志设计)
3. [事件循环与任务调度](#事件循环与任务调度)
4. [线程池管理](#线程池管理)
5. [并发控制策略](#并发控制策略)
6. [异常处理与超时](#异常处理与超时)
7. [性能优化](#性能优化)

## 简介
详细说明异步执行框架的完整文档。涵盖 is_async 标志的处理设计，说明同步和异步工具的统一接口设计。详细说明事件循环、任务调度、线程池管理、协程池等并发控制策略。介绍并发控制策略，包括限流、信号量使用和任务取消。说明异步异常处理模式，包括异常传播、超时处理和任务重试机制。涵盖性能优化，包括减少延迟和内存消耗。提供完整的代码示例，展示如何实现高性能的异步工具，包括实际实现和统一接口。

## is_async 标志设计

`mermaid
graph TB
    Tool["工具注册"] --> Check{"is_async?"}
    Check -- 是 --> Async["异步处理器"]
    Check -- 否 --> Sync["同步处理器"]
    Async --> EventLoop["事件循环"]
    Sync --> ThreadPool["线程池"]
    EventLoop --> Result["统一结果"]
    ThreadPool --> Result
`

### 注册示例
`python
# 异步工具注册
registry.register(
    name="async_tool",
    handler=async_handler,
    is_async=True
)

# 同步工具注册
registry.register(
    name="sync_tool",
    handler=sync_handler,
    is_async=False
)
`

**章节来源**
- [tools/registry.py:562-644](file://tools/registry.py#L562-L644)

## 事件循环与任务调度
Agent 使用 asyncio 事件循环管理异步任务：

`python
# 异步执行桥接
def _run_async(coro):
    """将异步协程桥接到同步上下文"""
    try:
        loop = asyncio.get_event_loop()
        if loop.is_running():
            # 嵌套事件循环：使用线程池
            with ThreadPoolExecutor() as pool:
                future = pool.submit(asyncio.run, coro)
                return future.result()
        else:
            return loop.run_until_complete(coro)
    except RuntimeError:
        return asyncio.run(coro)
`

## 线程池管理
同步工具通过线程池执行，避免阻塞事件循环：

`python
# 线程池配置
THREAD_POOL_SIZE = 10
thread_pool = ThreadPoolExecutor(max_workers=THREAD_POOL_SIZE)

# 同步工具执行
async def run_sync_in_thread(func, *args):
    loop = asyncio.get_event_loop()
    return await loop.run_in_executor(thread_pool, func, *args)
`

## 并发控制策略

### 信号量限制
`python
# 并发限制
TOOL_SEMAPHORE = asyncio.Semaphore(5)

async def limited_tool_execution(tool_func, args):
    async with TOOL_SEMAPHORE:
        return await tool_func(args)
`

### 任务取消
- 支持优雅取消正在执行的任务
- 取消时清理资源
- 通知用户任务状态

## 异常处理与超时

### 超时控制
`python
async def execute_with_timeout(coro, timeout=30):
    try:
        return await asyncio.wait_for(coro, timeout=timeout)
    except asyncio.TimeoutError:
        return tool_error("Tool execution timed out")
`

### 异常传播
- 异步异常自动捕获并转换为错误结果
- 详细错误日志记录
- 错误信息边界限制

## 性能优化
- 减少上下文切换
- 复用线程池
- 批量任务处理
- 内存使用优化

**章节来源**
- [agent/async_utils.py:1-100](file://agent/async_utils.py#L1-L100)
