# 工具注册机制

<cite>
**本文引用的文件**
- [tools/registry.py](file://tools/registry.py)
- [tools/skills_guard.py](file://tools/skills_guard.py)
</cite>

## 目录
1. [简介](#简介)
2. [注册表架构](#注册表架构)
3. [注册流程详解](#注册流程详解)
4. [安全控制机制](#安全控制机制)
5. [缓存与性能](#缓存与性能)
6. [错误处理](#错误处理)
7. [最佳实践](#最佳实践)

## 简介

工具注册机制是Sparkii Agent工具系统的核心，负责管理工具的生命周期。本文档详细说明ToolRegistry类的设计原理、注册参数的验证逻辑、工具元数据存储结构，以及插件覆盖策略和安全控制机制。

**关键概念：**
- **单例注册表**：全局唯一的ToolRegistry实例
- **自注册模式**：工具在模块导入时自动注册
- **安全覆盖**：插件需要显式授权才能覆盖内置工具
- **版本控制**：通过generation计数器实现缓存失效

## 注册表架构

### 核心数据结构

```python
class ToolRegistry:
    def __init__(self):
        # 工具存储：name -> ToolEntry
        self._tools: Dict[str, ToolEntry] = {}
        
        # 插件覆盖策略：module_namespace -> allowed
        self._plugin_override_policy: Dict[str, bool] = {}
        
        # 工具集检查函数：toolset -> check_fn
        self._toolset_checks: Dict[str, Callable] = {}
        
        # 工具集别名：alias -> canonical_toolset
        self._toolset_aliases: Dict[str, str] = {}
        
        # 线程安全锁
        self._lock = threading.RLock()
        
        # 版本计数器
        self._generation: int = 0
```

### ToolEntry元数据

```python
class ToolEntry:
    __slots__ = (
        "name",           # 工具唯一标识
        "toolset",        # 所属工具集
        "schema",         # OpenAI函数定义
        "handler",        # 处理函数
        "check_fn",       # 可用性检查
        "requires_env",   # 依赖环境变量
        "is_async",       # 异步标志
        "description",    # 描述
        "emoji",          # 图标
        "max_result_size_chars",  # 结果大小限制
        "dynamic_schema_overrides",  # 动态Schema覆盖
    )
```

## 注册流程详解

### 1. 模块导入阶段

当Python导入工具模块时，顶层代码自动执行：

```python
# tools/file_tools.py
from tools.registry import registry

# 定义Schema
READ_FILE_SCHEMA = {
    "name": "read_file",
    "description": "Read file contents",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {"type": "string"},
            "start_line": {"type": "integer"},
            "end_line": {"type": "integer"}
        },
        "required": ["path"]
    }
}

# 自动注册
registry.register(
    name="read_file",
    toolset="file",
    schema=READ_FILE_SCHEMA,
    handler=read_file_handler,
    emoji="📄"
)
```

### 2. register()方法详解

```python
def register(
    self,
    name: str,                    # 工具名称（必需）
    toolset: str,                 # 工具集（必需）
    schema: dict,                 # OpenAI Schema（必需）
    handler: Callable,            # 处理函数（必需）
    check_fn: Callable = None,    # 可用性检查函数
    requires_env: list = None,    # 依赖的环境变量
    is_async: bool = False,       # 是否异步
    description: str = "",        # 描述
    emoji: str = "",              # 图标
    max_result_size_chars: int | float | None = None,  # 结果大小限制
    dynamic_schema_overrides: Callable = None,  # 动态Schema覆盖
    override: bool = False,       # 是否允许覆盖
):
```

**参数验证逻辑：**

1. **名称冲突检测**
```python
existing = self._tools.get(name)
if existing and existing.toolset != toolset:
    if override:
        # 检查插件权限
        _owner = self._plugin_owner_of(handler)
        if _owner is not None and not self._plugin_override_policy.get(_owner, False):
            raise PermissionError(...)
    else:
        # 拒绝跨工具集覆盖
        logger.error("Tool registration REJECTED: ...")
        return
```

2. **工具集检查函数注册**
```python
if check_fn and toolset not in self._toolset_checks:
    self._toolset_checks[toolset] = check_fn
```

3. **版本计数器更新**
```python
self._generation += 1
```

### 3. 发现机制

```python
def discover_builtin_tools(tools_dir: Optional[Path] = None) -> List[str]:
    """Import built-in self-registering tool modules."""
    tools_path = Path(tools_dir) if tools_dir is not None else Path(__file__).resolve().parent
    
    # 加载缓存
    cache = _load_discovery_cache()
    
    module_names: List[str] = []
    for path in sorted(tools_path.glob("*.py")):
        # 排除特殊文件
        if path.name in {"__init__.py", "registry.py", "mcp_tool.py"}:
            continue
        
        # 缓存验证
        stat_key = (path.stat().st_mtime_ns, path.stat().st_size)
        cached = cache.get(str(path.resolve()))
        if cached and (cached[0], cached[1]) == stat_key:
            registers = bool(cached[2])
        else:
            registers = _module_registers_tools(path)
        
        if registers:
            module_names.append(f"tools.{path.stem}")
    
    # 导入模块
    for mod_name in module_names:
        importlib.import_module(mod_name)
    
    return module_names
```

**AST扫描逻辑：**

```python
def _module_registers_tools(module_path: Path) -> bool:
    """Return True when the module contains a top-level registry.register() call."""
    try:
        source = module_path.read_text(encoding="utf-8")
    except OSError:
        return False
    
    # 快速文本预过滤
    if "registry" not in source or "register" not in source:
        return False
    
    # AST解析
    try:
        tree = ast.parse(source, filename=str(module_path))
    except SyntaxError:
        return False
    
    return any(_is_registry_register_call(stmt) for stmt in tree.body)

def _is_registry_register_call(node: ast.AST) -> bool:
    """Return True when node is a registry.register(...) call expression."""
    if not isinstance(node, ast.Expr) or not isinstance(node.value, ast.Call):
        return False
    func = node.value.func
    return (
        isinstance(func, ast.Attribute)
        and func.attr == "register"
        and isinstance(func.value, ast.Name)
        and func.value.id == "registry"
    )
```

## 安全控制机制

### 1. 插件覆盖策略

插件覆盖内置工具需要显式授权：

```python
def register_plugin_override_policy(self, module_namespace: str, allowed: bool) -> None:
    """Bind a plugin module namespace to its operator opt-in for built-in override."""
    with self._lock:
        self._plugin_override_policy[module_namespace] = bool(allowed)
```

**配置方式：**
```yaml
# config.yaml
plugins:
  entries:
    my_plugin:
      allow_tool_override: true
```

### 2. 插件所有权验证

```python
def _plugin_owner_of(self, handler: Callable) -> Optional[str]:
    """Return the plugin module namespace that defined handler."""
    try:
        mod = handler.__globals__.get("__name__", "")
    except AttributeError:
        return None
    
    if mod in self._plugin_override_policy:
        return mod
    
    # 检查sparkii_plugins.命名空间
    if isinstance(mod, str) and mod.startswith("sparkii_plugins."):
        return mod
    
    return None
```

### 3. 反注销保护

防止插件通过注销-重新注册绕过覆盖检查：

```python
def deregister(self, name: str) -> None:
    """Remove a tool from the registry."""
    with self._lock:
        entry = self._tools.get(name)
        if entry is None:
            return
        
        # MCP工具豁免
        if not entry.toolset.startswith("mcp-"):
            caller_mod = self._caller_module()
            owner = self._plugin_owner_of(entry.handler)
            
            # 所有权检查
            caller_root = ".".join(caller_mod.split(".")[:2])
            owner_root = ".".join(owner.split(".")[:2]) if owner else ""
            same_plugin = bool(owner and caller_root == owner_root)
            
            if (caller_mod.startswith("sparkii_plugins.") 
                and not same_plugin 
                and not self._plugin_override_policy.get(caller_root, False)):
                raise PermissionError(...)
        
        del self._tools[name]
```

## 缓存与性能

### 1. 发现缓存

```python
def _discovery_cache_path() -> Optional[Path]:
    """Path of the tool-discovery verdict cache."""
    try:
        from sparkii_constants import get_sparkii_home
        return Path(get_sparkii_home()) / "cache" / "tool_discovery_cache.json"
    except Exception:
        return None

def _save_discovery_cache(cache: Dict[str, list]) -> None:
    """Best-effort atomic write of the discovery cache."""
    path = _discovery_cache_path()
    if path is None:
        return
    try:
        from utils import atomic_json_write
        path.parent.mkdir(parents=True, exist_ok=True)
        atomic_json_write(path, cache, indent=0)
    except Exception as e:
        logger.debug("Could not write tool discovery cache %s: %s", path, e)
```

**缓存格式：**
```json
{
  "/path/to/tools/file_tools.py": [1234567890, 12345, true],
  "/path/to/tools/terminal_tool.py": [1234567890, 67890, false]
}
```

### 2. check_fn TTL缓存

```python
_CHECK_FN_TTL_SECONDS = 30.0
_CHECK_FN_FAILURE_GRACE_SECONDS = 60.0
_CHECK_FN_CACHE_MAX = 512

_check_fn_cache: Dict[tuple[Callable, Optional[str]], tuple[float, bool]] = {}
_check_fn_last_good: Dict[tuple[Callable, Optional[str]], float] = {}
_check_fn_cache_lock = threading.Lock()

def _check_fn_cached(fn: Callable) -> bool:
    """Return bool(fn()), TTL-cached across calls."""
    now = time.monotonic()
    scope = check_fn_cache_scope()
    
    with _check_fn_cache_lock:
        _prune_check_fn_caches(now)
        cached = _check_fn_cache.get((fn, scope))
        if cached is not None:
            ts, value = cached
            if now - ts < _CHECK_FN_TTL_SECONDS:
                return value
    
    # 执行检查
    try:
        value = bool(fn())
    except Exception:
        value = False
    
    with _check_fn_cache_lock:
        if value:
            _check_fn_last_good[(fn, scope)] = now
            _check_fn_cache[(fn, scope)] = (now, True)
        else:
            # 故障宽限期检查
            last_good = _check_fn_last_good.get((fn, scope))
            if last_good and now - last_good < _CHECK_FN_FAILURE_GRACE_SECONDS:
                return True  # 返回上次成功的值
            _check_fn_cache[(fn, scope)] = (now, False)
    
    return value
```

### 3. 版本计数器

```python
# 注册时更新
self._generation += 1

# 查询时使用
def get_definitions(self, tool_names: Set[str], quiet: bool = False) -> List[dict]:
    """Return OpenAI-format tool schemas for the requested tool names."""
    result = []
    entries_by_name = {entry.name: entry for entry in self._snapshot_entries()}
    
    for name in sorted(tool_names):
        entry = entries_by_name.get(name)
        if not entry:
            continue
        
        # check_fn缓存
        if entry.check_fn:
            if entry.check_fn not in check_results:
                check_results[entry.check_fn] = _check_fn_cached(entry.check_fn)
            if not check_results[entry.check_fn]:
                continue
        
        # 动态Schema覆盖
        schema_with_name = {**entry.schema, "name": entry.name}
        if entry.dynamic_schema_overrides is not None:
            try:
                overrides = entry.dynamic_schema_overrides()
                if isinstance(overrides, dict):
                    schema_with_name.update(overrides)
            except Exception as exc:
                logger.warning(...)
        
        result.append({"type": "function", "function": schema_with_name})
    
    return result
```

## 错误处理

### 1. 注册错误

```python
# 跨工具集覆盖拒绝
logger.error(
    "Tool registration REJECTED: '%s' (toolset '%s') would "
    "shadow existing tool from toolset '%s'.",
    name, toolset, existing.toolset,
)
return  # 静默失败

# 插件权限拒绝
raise PermissionError(
    f"Plugin module {_owner!r} cannot override built-in tool "
    f"{name!r} without operator opt-in."
)
```

### 2. 分发错误

```python
def dispatch(self, name: str, args: dict, **kwargs) -> str | dict:
    """Execute a tool handler by name."""
    entry = self.get_entry(name)
    if not entry:
        return tool_error(f"Unknown tool: {name}")
    
    try:
        if entry.is_async:
            from model_tools import _run_async
            result = _run_async(entry.handler(args, **kwargs))
        else:
            result = entry.handler(args, **kwargs)
        return self._normalize_handler_result(name, result)
    except Exception as e:
        logger.exception("Tool %s dispatch error: %s", name, str(e))
        return tool_error(f"Tool execution failed: {type(e).__name__}: {e}")
```

### 3. 结果标准化

```python
def _normalize_handler_result(name: str, result):
    """Enforce the result shapes supported by the agent tool pipeline."""
    if isinstance(result, str):
        return _bound_json_error_result(result)
    
    if (isinstance(result, dict) and result.get("_multimodal") is True 
        and isinstance(result.get("content"), list)):
        return result
    
    # 不支持的结果类型
    return tool_error(
        f"Tool handler returned unsupported result type: {type(result).__name__}",
        error_type="tool_result_contract"
    )
```

## 最佳实践

### 1. 正确的工具注册

```python
from tools.registry import registry, tool_error, tool_result

# 完整的Schema定义
TOOL_SCHEMA = {
    "name": "my_tool",
    "description": "Clear description of what the tool does",
    "parameters": {
        "type": "object",
        "properties": {
            "required_param": {
                "type": "string",
                "description": "Parameter description"
            },
            "optional_param": {
                "type": "integer",
                "description": "Optional parameter",
                "default": 10
            }
        },
        "required": ["required_param"]
    }
}

# 健壮的处理函数
def my_tool_handler(args: dict, **kwargs) -> str:
    try:
        # 参数验证
        if not args.get("required_param"):
            return tool_error("Missing required parameter")
        
        # 业务逻辑
        result = perform_operation(args)
        
        # 成功返回
        return tool_result({
            "success": True,
            "data": result
        })
    except Exception as e:
        return tool_error(f"Operation failed: {e}")

# 注册
registry.register(
    name="my_tool",
    toolset="custom",
    schema=TOOL_SCHEMA,
    handler=my_tool_handler,
    check_fn=lambda: os.path.exists("/required/path"),
    requires_env=["MY_API_KEY"],
    emoji="🔧"
)
```

### 2. 插件覆盖内置工具

```python
# 1. 在config.yaml中授权
# plugins.entries.my_plugin.allow_tool_override: true

# 2. 插件中覆盖
from tools.registry import registry

def custom_browser_handler(args, **kwargs):
    # 自定义实现
    pass

registry.register(
    name="browser_navigate",  # 覆盖内置工具
    toolset="browser",
    schema=CUSTOM_BROWSER_SCHEMA,
    handler=custom_browser_handler,
    override=True  # 显式声明覆盖
)
```

### 3. 工具集管理

```python
# 检查工具集可用性
if registry.is_toolset_available("browser"):
    # 浏览器工具可用
    pass

# 获取工具集工具列表
tools = registry.get_tool_names_for_toolset("file")

# 注册工具集别名
registry.register_toolset_alias("fs", "file")
```

## 结论

工具注册机制通过集中式管理、严格的安全控制和智能缓存，为Sparkii Agent提供了强大而灵活的工具管理能力。开发者可以通过简单的API快速集成自定义工具，同时享受自动发现、权限验证和性能优化等高级特性。
