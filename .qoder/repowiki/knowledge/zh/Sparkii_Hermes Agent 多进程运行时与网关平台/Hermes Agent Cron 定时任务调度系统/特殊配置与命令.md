通过 `hermes gateway install` / `hermes gateway install --system` 安装为系统服务，gateway 后台线程每 60 秒调用 `tick()`；`hermes cron status` 读取 `ticker_heartbeat` / `ticker_last_success` 判断存活；`SPARKII_CRON_TIMEOUT` 控制作业不活跃超时并影响 one-shot claim TTL 与 CWD 锁等待上限。