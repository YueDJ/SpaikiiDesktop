运行环境要求 Node ≥20、npm ≥11.17.0；开发命令 `npm run start` 会先执行 `scripts/prebuild.mjs` 生成动态内容再启动 dev server；构建命令 `npm run build` 同样触发预构建，另有 `build:fast` 仅构建英文 locale；类型检查 `npm run typecheck` 使用 `tsc -p . --noEmit`；文档图校验 `npm run lint:diagrams` 调用 `ascii-guard lint --exclude-code-blocks docs`。