入口为 `python -m gateway.run` 或 `python cli.py --gateway`；首次 import 必须加载 `sparkii_bootstrap` 以在 Windows 上设置 UTF-8 stdio；Windows 下会调用 `_ensure_windows_gateway_venv_imports()` 把 venv site-packages 注入 sys.path 以便 MCP SDK 发现；可通过 `SPARKII_AUTO_CONTINUE_FRESHNESS` 环境变量控制中断后自动续接窗口。