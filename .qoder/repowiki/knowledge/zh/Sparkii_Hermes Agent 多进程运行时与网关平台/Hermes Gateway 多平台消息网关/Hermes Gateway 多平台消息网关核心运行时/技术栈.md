Python asyncio 异步运行时；HTTP 客户端使用 httpx（带 aiohttp-socks SOCKS 代理支持）；WebSocket 通信通过各平台 SDK（python-telegram-bot、discord.py、mautrix 等）及自研 `relay/ws_transport`；状态持久化基于 sparkii_state SQLite；TTS 输出通过 `tools.tts_tool` 生成 Ogg/MP3 并缓存到 `cache/audio`。