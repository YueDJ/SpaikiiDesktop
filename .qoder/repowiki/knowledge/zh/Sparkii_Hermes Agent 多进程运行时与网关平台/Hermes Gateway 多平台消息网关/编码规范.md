- 新增平台需在 platforms/ 下实现 base 抽象并在 platform_registry 中注册，保持与现有适配器一致的 adapter/send/send_or_update_status 接口
- 面向用户可见的消息在出站前统一经 run.py 中的 _sanitize_gateway_final_response / _redact_gateway_user_facing_secrets 做敏感信息与 provider 错误包装
- 平台特定常量与格式（如 Signal format、Yuanbao proto/sticker）拆分为独立子模块，由主适配器按需导入以避免冷启动开销
- 运行期横切关注点（会话、投递、钩子、命令、响应过滤、状态短语）以独立文件形式存在，被 run.py 和平台适配器共同引用而非内联