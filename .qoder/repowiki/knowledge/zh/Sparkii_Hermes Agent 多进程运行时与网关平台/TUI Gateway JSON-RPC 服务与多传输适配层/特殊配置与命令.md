入口为 `python -m tui_gateway.entry`（由 `sparkii --tui` 启动）；可通过环境变量 `SPARKII_TUI_GATEWAY_SHUTDOWN_GRACE_S` 控制优雅退出宽限、`SPARKII_TUI_RPC_POOL_WORKERS` 调整 RPC 线程池大小、`SPARKII_TUI_WS_ORPHAN_REAP_GRACE_S` 控制 WS 孤儿会话回收延迟、`SPARKII_TUI_SIDECAR_URL` 启用侧车 WS 广播、`SPARKII_TUI_GATEWAY_NO_FLUSH` 禁用 stdout flush 防半关闭管道阻塞。崩溃日志写入 `$SPARKII_HOME/logs/tui_gateway_crash.log`。