`sparkii update` 会调用 `seed_cache_from_checkout()` 把仓库自带的 `website/static/api/model-catalog.json` 直接拷贝到 `~/.sparkii/cache/model_catalog.json`，以绕过 Vercel 403 bot 防护；`sparkii model --refresh` 通过 `reset_cache()` 清除进程内缓存触发重新拉取。