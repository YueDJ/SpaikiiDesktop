Python 标准库 + httpx 进行 HTTP 调用；基于 `http.server.BaseHTTPRequestHandler` 的本地回调服务器实现 OAuth PKCE/device code 回调；使用 `fcntl`（Unix）/ `msvcrt`（Windows）实现跨平台文件级互斥锁；通过 `ThreadPoolExecutor` 并发探测 Z.AI 多个端点；`~/.sparkii/auth.json` 作为 JSON 格式的认证状态持久化。