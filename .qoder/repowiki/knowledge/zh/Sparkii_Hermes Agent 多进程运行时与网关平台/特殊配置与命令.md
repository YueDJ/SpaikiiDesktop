`pip install -e .` 或 `uv sync` 后通过 `sparkii`（CLI/TUI/Gateway）、`sparkii-agent`（独立 agent 循环）、`sparkii-acp`（ACP stdio 服务器）三个入口启动；`python run_agent.py` 可直接运行 agent 主循环；`python mcp_serve.py` 启动 MCP 服务器；`docker compose up` 使用根目录 `docker-compose.yml` 拉起 Gateway + 前端。