异步委派依赖 `state.db` 中的 `async_delegations` 表，首次连接时通过 `apply_wal_with_fallback` 自动迁移；Microsoft Graph 需设置 `MSGRAPH_TENANT_ID`/`MSGRAPH_CLIENT_ID`/`MSGRAPH_CLIENT_SECRET`；OpenRouter 需要 `OPENROUTER_API_KEY`；xAI 支持 `XAI_API_KEY`、`SPARKII_XAI_BASE_URL` 及 `~/.sparkii/auth.json` 中 `providers.xai-oauth.tokens` 或 `credential_pool.xai-oauth` 多账户池。