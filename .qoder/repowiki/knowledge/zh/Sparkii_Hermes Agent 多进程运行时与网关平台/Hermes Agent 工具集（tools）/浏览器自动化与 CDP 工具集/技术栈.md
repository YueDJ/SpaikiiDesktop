Python + websockets 直接连接 Chrome DevTools Protocol (CDP)；通过 `agent-browser` Node CLI 驱动本地 Playwright Chromium；Camofox 后端使用自托管 Node.js camofox-browser REST API；cloud 后端通过 `plugins/browser/<vendor>/` 插件机制（Browserbase / Browser Use / Firecrawl）注册到 `agent.browser_registry`。