基于 `mcp` Python SDK 的 `ClientSession`/`streamablehttp_client`/`sse_client`/`OAuthClientProvider`；HTTP 客户端使用 `httpx` 并配合 `tools.url_safety.create_ssrf_safe_async_client`；OAuth 状态文件采用 `os.open(O_EXCL, 0o600)` 原子写入；POSIX 下通过 `mcp_stdio_watchdog.py` 子进程 + `killpg` 实现父进程死亡回收。