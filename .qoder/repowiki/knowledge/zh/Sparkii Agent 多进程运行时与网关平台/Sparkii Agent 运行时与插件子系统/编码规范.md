- 通过包级 `_REGISTRY` + 懒导入 discover 机制实现可扩展的后端注册（见 `transports/__init__.py`、`secret_sources/registry.py` 等）
- 对重型/可选依赖采用函数内局部 import 包裹在 try/except ImportError 中，避免启动期失败（如 LSP 服务、各 transport 模块）
- 使用进程级单例 + 锁保护的懒初始化模式暴露全局服务（如 `lsp.get_service()`、`monitoring.get_emitter()`）
- 通过 `agent_init.py` 中的 `init_agent(agent, ...)` 大参数函数集中完成 AIAgent 属性装配，保持调用方薄包装
- 对外部回调（tool_progress/status/notice/event/reaction 等）以 `*_callback` 字段注入到 agent 实例，由运行时按需派发
- 监控与诊断事件经 `monitoring.emitter.emit` 异步投递，禁止在热路径直接写日志或发网络请求