启动时必须首先 `import sparkii_bootstrap` 以修复 Windows UTF-8 stdio，随后调用 `suppress_platform_ver_console()` 避免后台进程弹出 cmd 窗口；`--profile/-p` 必须在 argparse 之前被 `_apply_profile_override` 手动扫描并从 `sys.argv` 中剥离，否则后续子命令解析会失败。