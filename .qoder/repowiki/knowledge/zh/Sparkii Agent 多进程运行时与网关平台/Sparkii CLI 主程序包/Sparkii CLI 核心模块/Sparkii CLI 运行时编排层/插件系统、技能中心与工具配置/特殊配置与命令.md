`SPARKII_PLUGINS_DEBUG=1` 可开启插件发现调试日志；`SPARKII_BUNDLED_PLUGINS` 覆盖 bundled 插件目录；`SPARKII_ENABLE_PROJECT_PLUGINS` 启用项目级插件；Windows 下 post-setup 子进程通过 `CREATE_NO_WINDOW` 隐藏控制台窗口；`uv` 优先于 `pip` 执行 post-setup 包安装。