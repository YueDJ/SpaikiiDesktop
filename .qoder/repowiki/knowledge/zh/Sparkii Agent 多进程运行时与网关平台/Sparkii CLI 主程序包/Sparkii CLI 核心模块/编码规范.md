- 子命令以独立文件形式放在 `subcommands/` 下，并通过统一的注册机制在启动时被发现与挂载。
- 跨子命令共享的状态（配置、提供商、插件、会话）通过顶层模块的单例式对象访问，而非函数传参。
- Web 路由集中在 `web_routers/`，鉴权逻辑统一走 `dashboard_auth/` 提供的中间件与认证后端。
- 遥测指标通过 `observability/shared_metrics_contract.py` 定义的 schema 上报，保持跨子命令一致。