基于 `acp` SDK（Agent Communication Protocol）的 stdio JSON-RPC 传输；使用 `ThreadPoolExecutor(max_workers=4)` 在后台线程运行同步的 `AIAgent`；通过 `agent.async_utils.safe_schedule_threadsafe` 跨线程调度协程；持久化依赖 `sparkii_state.SessionDB`（SQLite）。