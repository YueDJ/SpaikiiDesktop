`web`：`npm run dev`（Vite）、`npm run build`（tsc -b && vite build）、`npm run test`（vitest run）。`ui-tui`：`npm run dev`（先 `build:ink` 再 `tsx --watch src/entry.tsx`）、`npm start`（直接运行）、`npm run build`（执行 `scripts/build.mjs`）、`npm run visual`（视觉回归脚本）。TUI 需要真实 TTY 环境运行，非 TTY 会直接退出。