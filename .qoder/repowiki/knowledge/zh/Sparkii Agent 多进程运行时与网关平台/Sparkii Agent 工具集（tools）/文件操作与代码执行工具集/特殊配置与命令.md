`SPARKII_DISABLE_FILE_STATE_GUARD=1` 可关闭跨代理文件状态守卫；checkpoint 默认从 `--checkpoints` 或配置开关启用，存储于 `~/.sparkii/checkpoints/store`；`execute_code` 仅在 POSIX 上可用（Windows 回退到 loopback TCP），且要求终端后端存在 Python 3；PDF 覆盖率提示依赖系统 `pdftotext`。