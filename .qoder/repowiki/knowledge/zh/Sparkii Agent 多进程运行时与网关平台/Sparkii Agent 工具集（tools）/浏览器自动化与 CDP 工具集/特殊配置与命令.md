本地模式需先执行 `npx agent-browser install --with-deps` 安装 Chromium 及系统依赖；Camofox 模式需设置 `CAMOFOX_URL=http://localhost:9377` 并启动 camofox-browser（npm start 或 Docker `jo-inc/camofox-browser`）；CDP 模式通过 `/browser connect` 设置 `BROWSER_CDP_URL` 或在 `config.yaml` 中配置 `browser.cdp_url`。