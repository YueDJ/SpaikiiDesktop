语音模式需额外安装可选依赖：`pip install sounddevice numpy` 或 `uv sync --extra voice`；Termux 下需安装 `pkg install termux-api` 及 Termux:API Android 应用；Docker/Modal/Vercel Sandbox 各自要求对应的认证凭据（如 `VERCEL_TOKEN`、`VERCEL_PROJECT_ID`、`VERCEL_TEAM_ID`）。