安装时通过 `tools/skills_sync.py` 将 bundled/optional skills 拷贝到 `~/.sparkii/skills/` 并写入 `.bundled_manifest`；Hub 功能需要配置 `GITHUB_TOKEN`/`gh` CLI/GitHub App 凭据以访问 tap 仓库；agent 创建的 skill 默认跳过 guard 扫描，需开启 `skills.guard_agent_created` 配置项才启用。