多数供应商通过 `sparkii tools` → 对应 section（Image Generation / Video Generation / Voice）选择 provider 并写入 `~/.sparkii/config.yaml`；STT/TTS 的可选依赖由 `tools.lazy_deps.ensure('tts.*'|'stt.*'|'wake.*')` 按需安装；wake word 在 macOS ARM64 上需额外 `pip install ai-edge-litert` 以启用 tflite 后端。