FAL.ai SDK（`fal_client` 懒加载）、Nous 托管工具网关（Bearer token + presigned upload）、httpx/requests/websockets 多种 HTTP/WS 客户端；本地推理使用 faster-whisper、openwakeword (ONNX/tflite)、sherpa-onnx、pvporcupine、edge_tts、piper-tts、kittentts、sounddevice+numpy。