Python 标准库（`threading`、`contextvars`、`subprocess`、`difflib`、`importlib.metadata`、`packaging`）；依赖 `uv` 作为首选包管理器并在不可用时回退到 `python -m pip` + `ensurepip`；通过 `sparkii_cli.config` 与 `agent.secret_scope` 集成配置与多租户密钥作用域。