Python 测试通过 `scripts/run_tests.sh` / `scripts/run_tests_parallel.py` 以每文件一进程方式运行；单个文件可直接 `python -m pytest tests/<file>.py` 执行。TypeScript 测试在 `tests-js/` 内通过 `npm test`（即 `vitest run`）或 `npm run check`（类型检查 + 测试 + lint）运行。