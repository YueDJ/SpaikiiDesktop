Electron 40 + Vite 8 + React 19 + TypeScript 6（desktop）；Tauri 2 + Rust（bootstrap-installer）；nanostores 作为轻量状态管理；Tailwind CSS v4 + Radix UI；Vitest + Playwright 用于单元测试与 E2E；electron-builder 负责多平台打包与 macOS 代码签名。