Python 插件体系，基于 ABC（`ProviderProfile`、`MemoryProvider`、`WebSearchProvider`、`BrowserProvider`、平台 adapter）+ `plugin.yaml` 元数据声明；并发安全使用 stdlib `threading.Lock` 实现的单例槽；部分重型或阻塞依赖（如 `ddgs`）通过子进程隔离执行以避免 GIL 死锁。