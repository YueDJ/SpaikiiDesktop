"""Sparkii core foundation.

This package is the narrow-waist foundation: the agent kernel and the extension
infrastructure (plugins, skills, MCP, toolsets) that everything else builds on.
Surface packages (gateway, sparkii_cli, frontends) depend on this package, never
the other way around.
"""
